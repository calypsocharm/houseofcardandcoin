# dependencies
node_modules/

# secrets (kept locally only — never commit)
.hocc-vps-secret
.hocc-askpass.bat
.hocc-members-secret
askpass.exe

# live member data + uploads (runtime, not source)
app/data/
app/uploads/

# original camera-resolution artwork (kept locally for re-crops; the web
# version lives in assets/img/)
20260531_144549.jpg

# scratch output from the retired build.js — never the real pages
build-output/

# logs
*.log

# session store (runtime, not source)
app/sessions/
